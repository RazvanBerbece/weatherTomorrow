# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'weatherTomorrow' do
  # Comment the next line if you don't want to use dynamic frameworks
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Alamofire', '~> 5.0.0-rc.3'
  # use_frameworks!
  end

  # Pods for weatherTomorrow
  
  target 'weatherTomorrowTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'weatherTomorrowUITests' do
    # Pods for testing
  end
  
  workspace 'weatherTomorrow'
